version: '3.9'

services:
  db:
    image: postgres:17
    container_name: metal_db
    ports:
      - 5432:5432
    volumes:
      - ~/postgresql-metal-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=metal
      - POSTGRES_USER=metal
      - POSTGRES_DB=metal